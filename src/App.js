import { BrowserRouter, Routes, Route } from 'react-router-dom';
import Router from './router';
import './App.css';

function App() {
  return (
    <BrowserRouter>
      <Routes>
      </Routes>
      <Router />
    </BrowserRouter>
  );
}

export default App;
